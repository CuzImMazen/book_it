import 'package:flutter/material.dart';

class CanceledTabView extends StatelessWidget {
  const CanceledTabView({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: const Text("Cancelled"));
  }
}
